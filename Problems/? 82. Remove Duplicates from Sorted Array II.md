# Python

## Wrong Answer!!!!!
```
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def deleteDuplicates(self, head):
        """
        :type head: ListNode
        :rtype: ListNode
        """
        dummy=ListNode(0,head)
        left = dummy
        right = dummy.next

        while right.next:
            if right.val != right.next.val:
                right = right.next
                left = left.next
            elif right.val == right.next.val:
                right = right.next
                left.next = right.next

        return dummy.next
```
